CREATE TABLE IF NOT EXISTS activ
    (id INTEGER PRIMARY KEY AUTOINCREMENT, 
     username VARCHAR(100),
     info VARCHAR(20),
     about VARCHAR(200),
     whatelse VARCHAR(200),
     sphere VARCHAR(30)
    )